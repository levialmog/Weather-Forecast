/**
 * The function is a component which returns an empty page.
 * @returns {JSX.Element} An empty page.
 * @constructor
 */
function NoPage() {
    return (
        <h1 className="text-center">Nothing In Here...</h1>
    );
}

export default NoPage;